import React from 'react'

import { View, Text, TouchableOpacity } from 'react-native'
import styled from 'styled-components/native'
import Pdf from 'react-native-pdf';

const BaseDetail = ({ assetPath }) => {
    return <View>

        <Text>wut???</Text>
    </View>
}